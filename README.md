# MorphGo
A Reference Architecture for CodeAct (Code-as-Action) Agents written in Go. Dynamically inspects, generates, and executes Go code to transform structured files (XML, CSV, JSON, XLSX) with self-correcting loops.
